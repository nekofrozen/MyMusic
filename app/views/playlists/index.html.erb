<h1>Playlists</h1>

<%= link_to 'New Playlist', new_playlist_path, class: 'button small' %>
<br/><br/>

<% @playlists.each do |playlist| %>
  <%= link_to (image_tag playlist.image_url(:thumb)), playlist %>
    <%= link_to  playlist.name, playlist %>
<% end %>

<br><hr>
<%= link_to 'Back Audio', audiotracks_path %>
